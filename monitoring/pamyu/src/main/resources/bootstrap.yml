spring:
  application:
    name: pamyu
  boot:
    admin:
      context-path: pamyu
      notify.mail.to: "kowlikus@yahoo.com"
      routes.endpoints: env,metrics,dump,jolokia,info,configprops,trace,logfile,refresh,flyway,liquibase,heapdump,loggers,auditevents,hystrix.stream,activiti
  cloud:
    config:
      discovery:
        enabled: true
        service-id: mamamoo
server:
  port: -1
  context-path: pamyu
eureka:
  client:
    eureka-server-u-r-l-context: charisma
    register-with-eureka: true
    fetch-registry: true